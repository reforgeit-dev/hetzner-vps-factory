APP_ID={{ lookup('pipe', 'openssl rand -hex 16') }}
APP_NAME=Coolify
APP_KEY=base64:{{ lookup('pipe', 'openssl rand -base64 32') }}

DB_USERNAME=coolify
DB_PASSWORD={{ lookup('pipe', 'openssl rand -base64 32') }}

REDIS_PASSWORD={{ lookup('pipe', 'openssl rand -base64 32') }}

PUSHER_APP_ID={{ lookup('pipe', 'openssl rand -hex 32') }}
PUSHER_APP_KEY={{ lookup('pipe', 'openssl rand -hex 32') }}
PUSHER_APP_SECRET={{ lookup('pipe', 'openssl rand -hex 32') }}

ROOT_USERNAME=
ROOT_USER_EMAIL=
ROOT_USER_PASSWORD=

REGISTRY_URL=ghcr.io
